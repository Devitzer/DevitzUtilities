const help = require('./SupportCmds/help')
const version = require('./SupportCmds/version')
const versionString = require('./SupportCmds/versionString')

module.exports = {
    help,
    version,
    versionString
}